var amount = (5+5)*11;

if(amount < 100){
	console.log("amount is less then 100");
} else {
	console.log("amount is more then 100");
}

//assignment =
// equality ==
// Not equal !=
// Strict equality ===
//Not Strict Equal !==
// Greater then >
// Less Then <
// greater then equal to >=
// less then equal to <=
// must be true or false
// && logical and
// (a === b && c === d)
// ((a === b) && (c === d))
// or logic 
// (a === b or c === d)
